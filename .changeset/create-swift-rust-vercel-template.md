---
"create-swift-rust": minor
---

Generate a `vercel.json` preconfigured for the Swift Rust build output (`outputDirectory: ".vercel/output"`), so new apps deploy to Vercel out of the box. Bump the generated `swift-rust` dependency from `0.1.0` to `^0.2.0`. Add a "Deploy to Vercel" section to the generated README.
