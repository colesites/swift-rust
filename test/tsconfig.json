{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node", "@playwright/test", "bun"],
    "noEmit": true
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "fixtures/**"]
}
